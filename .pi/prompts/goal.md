---
description: Run the active pi-iterative-goal production-certification contract
argument-hint: "[requested outcome within the active goal]"
---

Read `goal.md`, `northstar.md`, `state.md`, and `learnings.md` completely before
acting. `goal.md` is the living session contract; the other files respectively
hold strategic authority, the current proof boundary, and durable truths.

Treat `$ARGUMENTS` as the requested outcome inside that contract. If no argument
is supplied, prosecute the highest-priority unblocked lane. An argument cannot
replace the objective, weaken an invariant, or grant production/destructive
authority.

Use the durable research → plan → implement → validate loop and its goal tools.
Keep at most one task in progress. Preserve runId and phaseAttemptId identities,
exact budgets, artifacts, and proof labels. Material work receives independent
adversarial and architecture review. Only trusted checks and the independent
evaluator may declare completion.

After each milestone, update `goal.md` and its append-only evolution log. Add
durable reproduced lessons to `learnings.md`; replace `state.md` at pauses,
compaction, handoff, before and after long external runs, and session end.
Ensure the quartet lands on the visible PR head.
